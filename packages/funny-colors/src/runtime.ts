// createBackground — public runtime entry point stub.
// Full implementation lives in Phase 5.

import type { BackgroundInstance, GraphConfig } from '@funny-colors/core'

export function createBackground(
  _canvas: HTMLCanvasElement,
  _config: GraphConfig,
): BackgroundInstance {
  throw new Error('createBackground: not yet implemented')
}
